<view class="container">
    <view class="game-container">
        <text class="title">Hangman Game</text>
        <view class="difficulty">
            <button bindtap="setDifficulty" data-difficulty="easy">Easy</button>
            <button bindtap="setDifficulty" data-difficulty="medium">Medium</button>
            <button bindtap="setDifficulty" data-difficulty="hard">Hard</button>
        </view>
        <canvas canvas-id="hangmanCanvas" style="width: 100%; height: 300rpx;"></canvas>
        <text id="word-display" class="word-display">{{wordDisplay}}</text>
        <text id="guesses" class="guesses">Guesses: {{guesses}}</text>
        <view class="alphabet">
            <block wx:for="{{alphabet}}" wx:key="letter">
                <button bindtap="guessLetter" data-letter="{{item}}" disabled="{{guessedLetters.indexOf(item) !== -1}}">{{item}}</button>
            </block>
        </view>
        <button bindtap="showHint" id="hint-btn">Get Hint</button>
        <text id="message" class="message">{{message}}</text>
        <text id="score" class="score">Wins: {{wins}} | Losses: {{losses}}</text>
        <button bindtap="newGame">New Game</button>
    </view>
</view>
